{
  "name": "realtime-kanban",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:seed": "pnpm --filter api db:seed",
    "db:setup:sqlite": "pnpm --filter api db:setup:sqlite",
    "db:setup:postgres": "pnpm --filter api db:setup:postgres"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
